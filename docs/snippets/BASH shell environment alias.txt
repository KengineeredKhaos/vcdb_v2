# BASH shell alias shortcuts

# alias to start a python virtual environment from the current directory


# Launch vcdb (cd, activate venv, run app)
vcdb() {
  local proj="$HOME/Documents/App_Dev/vcdb-v2"
  local env="dev"          # default env
  local port=""            # no default; use app default unless provided
  local env_set=""

  # --- parse args ---
  # Supports:
  #   vcdb                 -> env=dev, no --port
  #   vcdb test            -> env=test
  #   vcdb test 5001       -> env=test, port=5001
  #   vcdb --env test --port 5001
  #   vcdb -e test -p 5001


# launch a VCDB test environment without starting a server

# examples:
# vcdbt shell
# vcdbt seed seed-foundation
# vcdbt dev policy-health
