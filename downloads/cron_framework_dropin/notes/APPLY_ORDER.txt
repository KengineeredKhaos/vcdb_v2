1) Add new files first:
   - app/slices/admin/runtime_cron.py
   - app/slices/admin/registry_cron.py
   - app/slices/admin/jobs_backup.py
   - app/slices/admin/services_cron.py
   - app/cli/cron.py
   - app/slices/admin/templates/admin/cron.html

2) Patch existing Admin files:
   - admin/models.py      -> add CronRun and CronLock
   - admin/services.py    -> delegate get_cron_page() to services_cron

   Reference files:
   - notes/admin_models.patch.diff
   - notes/admin_services.patch.diff
   - notes/admin_models.modified.py
   - notes/admin_services.modified.py

3) Register the CLI command.
   See notes/cli_registration.txt

4) Create the migration for new Admin tables.
   New tables needed:
   - admin_cron_run
   - admin_cron_lock

5) Land tests:
   - tests/slices/admin/test_runtime_cron.py
   - tests/slices/admin/test_jobs_backup.py
   - tests/slices/admin/test_services_cron.py

6) First focused test pass:
   pytest tests/slices/admin/test_runtime_cron.py \
          tests/slices/admin/test_jobs_backup.py \
          tests/slices/admin/test_services_cron.py -q

7) Manual smoke test after CLI registration:
   flask cron list
   flask cron run backup.daily --unit-key 2026-03-30

Notes:
- backup.daily currently supports sqlite only.
- external backup copy is optional unless CRON_BACKUP_REQUIRE_EXTERNAL=True.
- routes.py does not need a change if it already calls svc.get_cron_page().
